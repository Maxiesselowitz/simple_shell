#include <stdio.h>

/**
 * main - a program to pass betty checks
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int r;

	for (r = 0; r < 10; r++)

	{

		printf("Hello, Betty! %d\n", r);

	}

	return (0);
}

